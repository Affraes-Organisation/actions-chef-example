---
driver:
  name: exec

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-18.04

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/smoke/default
    attributes:
      chef_client:
        chef_license: accept
